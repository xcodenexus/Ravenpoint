# Copy this file to .env.local and fill in your keys.
# .env.local is gitignored — never commit real keys.

# Required for AI Analyst (Settings → API Keys → Anthropic API Key)
ANTHROPIC_API_KEY=

# Optional OSINT integrations
VIRUSTOTAL_API_KEY=
SHODAN_API_KEY=
ABUSEIPDB_API_KEY=
RF_API_TOKEN=
